# Dashboard 重构变更报告

> 范围说明：本报告记录目前在 Codex 副本中的改动。此前按你的明确要求推送过副本分支；没有合并、没有触碰本体目录。

## 1. 总体新增内容

这次主要把原来“封面之后直接进入学习页”的结构，改成了更接近产品构思里的三层结构：

1. 封面：一个极简温柔的进入页。
2. Dashboard：作为进入学习前的玄关，承载资料库、日历、便签、成长和画像入口。
3. 学习页：保留原来的 PDF 左侧 + AI 右侧主学习体验。

## 2. 新增 Dashboard 页面

新增文件：

- `shared/layout/DashboardScreen.tsx`

新增能力：

- 资料库作为 dashboard 主内容。
- 日历作为 dashboard 侧边入口。
- 便签作为 dashboard 侧边入口。
- “我的成长”入口，先展示当前可从本地/云端数据推出来的基础统计。
- “关于我”入口，作为长期画像笔记本的编辑入口。
- “能量补给”入口，承载原本学习页里的 TaskHug 和 ChatHug。
- 迎接者区域：显示“未来的你”、一句迎接话、最近状态描述。
- 迎接话和画像字段可在本地编辑，并保存到 `localStorage`。

现在“关于我”已经接入长期画像笔记本；未登录时本地保存，登录后会同步到 Firestore。

## 3. 资料库卡片改动

位置：

- `shared/layout/DashboardScreen.tsx`
- `lib/pdf/pdfUtils.ts`

改动：

- 每个资料库卡片对应一个 PDF。
- 卡片封面现在使用 PDF 第一页渲染出来的图片。
- 封面渲染改走和“打开 PDF”一致的取文件通路，避免直接用远程 URL 渲染失败后一直显示占位图。
- 第一页预览失败时，才退回占位封面。
- 点击卡片时会显示“打开中”的转圈反馈。
- 点击卡片后会立即切到学习页，不再让用户留在 dashboard 等待整份 PDF 解析完成。

新增 PDF 工具：

- `renderPdfFirstPagePreview`
- `renderPdfFirstPagePreviewFromUrl`

用途是只渲染 PDF 第一页，用于 dashboard 卡片封面。

## 4. 封面页改动

位置：

- `shared/layout/WelcomeScreen.tsx`

改动：

- 替换原来的英文 quote 和橙色按钮风格。
- 改成中文、温柔、低门槛的“玄关”视觉。
- 保留时钟 + 一句话 + “进入”按钮。
- 点击“进入”后进入 dashboard，而不是直接进入学习页。

## 5. App 入口与页面流转改动

位置：

- `App.tsx`

新增状态：

- `shellMode: 'dashboard' | 'study'`

页面流转现在是：

- 首次打开：封面。
- 点“进入”：dashboard。
- dashboard 点 PDF 卡片：进入学习页并恢复/加载 PDF。
- 本地上传 PDF：处理成功后进入学习页。
- 学习页 header 点 dashboard 图标：回 dashboard。

另外：

- 云端 PDF 恢复失败时，会退回 dashboard，并提示恢复失败。
- 云端 PDF 恢复成功后，会停留在学习页。

## 6. 学习页 Header 改动

位置：

- `shared/layout/Header.tsx`

改动：

- 增加一个返回 dashboard 的图标按钮。
- 这个按钮只负责在学习页和 dashboard 之间切换。
- 原有上传、学习工具、考试复习、背景音、更多菜单等功能保留。
- 修复右上角“更多”菜单被学习区浮层遮挡的问题：Header 和菜单层级已提高，菜单会盖在“导出笔记版 PDF”等学习区浮层上方。
- 删除学习页“学累了 / 休息”子菜单中的“能量补给”，能量补给现在只作为 Dashboard 的正式入口。

## 7. 学习页左侧栏改动

位置：

- `shared/layout/Sidebar.tsx`

改动：

- 删除左侧栏顶部的“页面 / 云端 / 日历 / 便签”标签栏。
- 学习页左侧现在直接显示当前 PDF 的页面缩略图。
- 云端、日历、便签已经迁移到 dashboard，不再在学习页左侧重复出现。

保留内容：

- 页面缩略图。
- 页面跳转。
- 页面标记筛选。
- 当前 PDF 的复习入口。

## 8. Gemini 初始化防白屏改动

位置：

- `services/geminiService.ts`
- `services/imageGen.ts`

问题：

之前没有配置 `API_KEY` 时，应用一打开就会初始化 Gemini client，导致整页白屏。Dashboard 本身不需要 Gemini，但也会被这个初始化错误拖垮。

改动：

- Gemini client 改为懒加载。
- 只有真正调用 AI 功能时，才检查 `API_KEY`。
- 这样 dashboard、封面、基础资料库等非 AI 页面可以正常打开。

## 8.1 页面工具误失败修复

位置：

- `services/geminiService.ts`
- `App.tsx`
- `features/reader/deep-read/ExplanationPanel.tsx`

改动：

- 页面工具继续使用当前 Gemini 模型，但 prompt 已从“独立精读”改为“领读里的局部工具”。
- 讲解结果现在会先按标准 JSON 解析；失败后会清理代码块、截取 `{...}` 再解析。
- 如果模型返回了缺字段 JSON，会用已有字段拼出讲解，不再直接显示失败。
- 如果模型返回了普通可读文本，会显示为“讲解草稿”。
- 真正失败时，只记录当前页错误状态，不把失败文案写进讲解缓存。
- 失败态增加了更柔和的提示和轻量动画，右上角重新生成按钮仍然保留。

## 8.2 领读主线 + 页面工具重构

位置：

- `App.tsx`
- `shared/layout/Header.tsx`
- `features/reader/skim/SkimPanel.tsx`
- `features/reader/deep-read/ExplanationPanel.tsx`
- `services/geminiService.ts`
- `features/reader/notebook/Notebook.tsx`
- `data/studyFlowTemplates.ts`
- `lib/prompts/systemPrompts.ts`

改动：

- 用户界面里的“略读”改成“领读”；“略读 1 / 新建略读”改成“领读 1 / 新建领读”。
- 新打开资料默认进入领读配置区，不再默认进入旧精读页，也不再打开后自动生成单页讲解。
- 顶部按钮在领读时显示“页面工具”；点进去后才使用当前页工具。
- 旧 `deep` 状态仍保留为兼容层，但 UI 上呈现为“页面工具”，不再作为独立精读模式强调。
- 页面工具新增三个手动动作：`讲这一页`、`生成本页笔记`、`这页怎么考`。
- 页面工具结果按“页面 + 工具类型”分别缓存，避免本页笔记覆盖本页讲解。
- 页面工具会把当前领读状态、module 数、节奏、页码范围、学习地图和最近领读对话传给 Gemini。
- “生成本页笔记”结果可以保存到学习手帐；手帐里旧“精读笔记 / 略读笔记”文案改为“页面笔记 / 领读笔记”。
- 学习计划模板里“略读 / 精读当前页”改为“领读 / 页面工具”。

和之前的区别：

- 之前：略读和精读像两个平行模式，精读页会自动生成单页讲解。
- 现在：领读是主学习路径；页面工具只在需要时手动触发，负责局部放大、笔记沉淀和考试视角。

验收清单：

1. 打开 `localhost:3004`。
2. 打开一份新 PDF，确认默认进入“领读”配置区。
3. 选择 module 数和节奏，确认仍能开始领读。
4. 在顶部点“页面工具”，确认右侧出现 `讲这一页 / 生成本页笔记 / 这页怎么考`。
5. 不点按钮时，确认不会自动生成单页讲解。
6. 点“讲这一页”，确认开始生成当前页解释。
7. 点“生成本页笔记”，确认结果可以点“保存到学习手帐”。
8. 点“这页怎么考”，确认输出是当前页考点/问法/易错点。
9. 点“回到领读”，确认回到领读会话。
10. 恢复旧资料，确认旧页面工具缓存和旧领读记录不丢。

## 9. 已验证内容

已运行：

```bash
npm run build
```

结果：

- 构建通过。
- 仍有 Vite 的 chunk 体积提示，但不是错误。

已手动检查过：

- 封面能渲染。
- 点击“进入”能进入 dashboard。
- Dashboard 的资料库、日历、便签、我的成长、关于我入口能切换。
- 学习页可以从 header 回 dashboard。
- 学习页左侧标签栏已删除。

## 10. 当前未做内容

目前还没有做：

- 成长报告完整可视化。
- Live2D / 动态角色。
- 合并、提交到本体。
- 当前这批后续改动还没有再次提交或推送。

## 11. 长期画像笔记本第一版

新增/改动位置：

- `types.ts`
- `services/profileNotebookService.ts`
- `services/geminiService.ts`
- `App.tsx`
- `shared/layout/Header.tsx`
- `shared/layout/DashboardScreen.tsx`

新增数据结构：

- `LearnerProfileNotebook`：正式长期画像笔记本。
- `StudyWitnessSession`：一次学习的原始行为录像。
- `ProfileNotebookUpdateSuggestion`：Gemini 生成、等待用户确认的画像更新建议。

画像笔记本目前包含：

- 哪里顺 / 哪里卡
- 专注能撑多久
- 卡住时的反应
- 什么时候状态最好
- 最近怎么样

实现策略：

- 未登录时画像和录像保存在本地。
- 登录后画像同步到 Firestore：`users/{uid}/profileNotebook/main`。
- 登录后行为录像保存到 Firestore：`users/{uid}/studyWitnessSessions/{sessionId}`。
- 待确认建议可保存到 Firestore：`users/{uid}/pendingProfileSuggestions/{suggestionId}`。
- 如果关闭页面时只能来得及写本地记录，下次打开/登录后会把本地记录补传到云端。

学习页新增：

- Header 下方增加明显的“本次学习”状态条，按钮靠右显示 `开始学习 / 结束本次学习`。
- 必须手动点击开始，才会记录 witness。
- 点击结束后弹出“本次学习小结”。
- 弹窗展示本次记录到的现象，以及 Gemini 给出的画像更新建议。

记录的 witness 内容只包含现象：

- 文件名
- 开始/结束时间
- 总时长
- 有效页面停留时长
- 每页停留时间
- 中途离开/回来次数和时长
- 最后停在哪一页

不会记录或推断：

- 懒
- 害怕
- 逃避原因
- 自律评价
- 用户聊天内容

Gemini 接入方式：

- Gemini 只生成“建议版笔记本”。
- 不会静默写入正式画像。
- 用户可以在小结弹窗里编辑建议内容。
- 用户点击“保存当前建议”后，才会覆盖正式画像。
- 用户也可以点“稍后再看”或“跳过这次”。

异常结束处理：

- 如果已经点了开始，但直接关闭/刷新页面，会保存为 `abandoned` witness。
- 从学习页回 dashboard 时，如果本次学习还没结束，也会保存为未正常结束。
- 未正常结束记录会先本地落盘，登录后补传到 Firestore。
- 未正常结束不会自动触发 Gemini 更新。

最近状态趋势：

- Gemini 更新第 5 栏时，按“最近 5 次学习 + 最近 21 天”窗口处理。

## 12. 联合复习 V1-V3

新增/改动位置：

- `types.ts`
- `services/firebase.ts`
- `services/geminiService.ts`
- `shared/layout/DashboardScreen.tsx`

V1：联合复习包

- Dashboard 新增“联合复习”入口。
- 可以从云端资料里选择多个 PDF，组合成一个复习包。
- 每份资料可以标记为 `Lecture`、`Reading`、`Article` 或 `其他`。
- 支持生成一份“复习说明”，把 lecture 主线和 readings/article 的补充证据放在一起看。
- 复习包保存到 Firestore：`users/{uid}/jointReviewPacks`。

V2：联合领读

- 复习包详情里新增“联合领读”标签。
- 可以用对话方式让 AI 带着多份材料一起复习。
- 默认强调：lecture 是主线，reading/article 是解释、证据、反例或考试加分点。
- 新增快捷按钮：`开始联合领读`、`继续下一小段`、`只讲 lecture`、`补 reading 证据`。
- 联合领读对话会保存回同一个复习包，刷新后可继续。

V3：考试整合

- 复习包详情里新增“考试整合”标签。
- 可以一键生成考前版本，把 lecture 主线和 readings/article 证据整理成：
  - 可能考法
  - 最小答案模板
  - lecture + reading 对照表
  - 容易混的点
  - 10 分钟考前速览
  - 自测题
- 考试整合结果会保存到同一个复习包，下次打开还能看到。
- 可以重新生成，不会影响原来的复习说明和联合领读对话。

## 13. 安全边界

当前所有改动都发生在 Codex 副本：

```text
/Users/haoshengliu/.codex/worktrees/5bcb/class-skip-v2
```

没有对本体执行推送、合并或写入操作。

## 14. Lecture 分段式学习（唱片）

这次为 Lecture 领读新增第二种学习方式，没有替换原来的整段式领读。

创建领读时现在可以选择：

- `整段式学习`：继续使用原来的完整对话流。
- `分段式学习（唱片）`：先生成整份 Lecture 路线，再按 Module 或 Part 展示成横向唱片架。

唱片式学习新增：

- 每张唱片显示范围首页缩略图、Module/Part、页码范围、两句话梗概与学习状态。
- 一次一个 Module 时，每个 Module 对应一张唱片；一次一个 Part 时，Part 数量按内容决定，没有 Part 的 Module 会直接成为唱片。
- 唱片架支持左右箭头、触控板横滑、鼠标拖动和 Module 快速筛选，只渲染当前附近的卡片。
- 第一次打开唱片会跳到范围首页；再次打开会恢复上次停留页、独立对话和最后一条消息。
- 唱片内仍可自由查看整份 PDF，原有便签、注释、问答和两种 PDF 导出保持不变。
- 当前对话默认严格围绕这张唱片；只有用户明确要求联系其他部分时，AI 才使用整份 PDF 的全局视野。
- 追问、重讲和举例永久附属于当前唱片，不会被误认为新的 Module/Part。
- 离开唱片时自动保存已讲清内容、未解决问题和停留位置；摘要失败不会影响原对话保存。
- 只有手动点击“我学完了”才会完成，也可以撤销；跳过前置唱片只提醒，不阻止。
- 整段式和唱片式分别保存自己的阅读位置与对话，来回切换不会相互覆盖。

路线保护：

- 生成路线时保留逐页文本和明确的应用内页码。
- 创建唱片架前检查越界、倒序、缺页、异常重叠以及 Part 是否位于所属 Module 内。
- 首次校验失败会把具体错误交给 AI 自动修复一次；再次失败则不创建错误唱片架。

云端保存方式：

- 领读主文档只保存路线、顺序和轻量状态。
- 每张唱片的长对话保存到独立子文档，避免全部对话挤进一个文档。
- 重新规划会清除旧唱片状态、对话和摘要，但不会删除整段式领读、PDF 便签、页面评论或导出记录。

兼容性：

- 旧领读没有学习方式字段时，自动按整段式打开。
- Paper 和文章目前继续使用整段式，唱片式第一版只对 Lecture 开放。
