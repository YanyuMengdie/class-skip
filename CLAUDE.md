# Claude Code 工作指引

本项目使用四档开发流程，详见 docs/DEV_SOP.md。

## 收到任务时必须先做的事

1. 阅读 docs/DEV_SOP.md 判断本次任务的档位
   - Think First：纯产品认知问题，不写代码
   - Playground：实验性、视觉性，开 branch 玩
   - Quick Fix：明确范围的 bug 或小改动
   - Core：架构性、跨模块、有数据流影响的改动

2. 阅读 CONTEXT.md 了解项目现状
   - 重点关注"产品事实修正"那一节
   - 不要靠记忆，靠 view 文件

3. 如果是 Core 任务，找对应的 docs/inquiries/UPDATE_*.md
   - 严格按"改动范围"和"不改什么"执行
   - 没有 inquiry 文档就停下来问我

4. 列出改动计划，等我确认后再动手
   - 改哪些文件
   - 改哪些行号或函数
   - 不要列完就开始改

## 红线（永远不做）

- 不自由发挥，不顺手重构
- 不跳过手动测试
- 一改一 commit，不混合多个改动
- 不在一次会话里同时做多个不相关任务
- 不修改 scope 外的文件，即使"看起来应该改"
- 任何意外发现先停下来报告，不自作主张

## 验证清单（每次必跑）

- npm run dev 启动无报错
- TypeScript 编译无错误
- 上传 PDF 进入精读模式正常
- 切换到略读模式正常
- 进入备考工作台正常
- 笔记本能打开